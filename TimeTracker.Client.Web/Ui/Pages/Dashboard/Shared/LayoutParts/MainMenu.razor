@using LumexUI.Common
@inherits BaseReactiveComponent

<section class="border-b border-slate-200 bg-slate-50/70">
    <div class="px-4 py-3 sm:px-6 lg:px-8">
        @{
            var availableNavItems = AvailableNavItems.ToList();
            var primaryItems = availableNavItems
                .Where(item => item.GroupName == null && !string.IsNullOrEmpty(item.Name))
                .AsQueryable();
            var groupedItems = availableNavItems
                .Where(item => item.GroupName != null)
                .GroupBy(i => i.GroupName)
                .AsQueryable();
            var settingsItem = availableNavItems.FirstOrDefault(item => string.IsNullOrEmpty(item.Name));
        }
        <div class="flex flex-col gap-3">
            <div>
                <p class="text-[11px] font-semibold uppercase tracking-[0.18em] text-slate-500">@DashboardLocalizer["WorkspacePages"]</p>
                <p class="mt-1 text-sm text-slate-500">@DashboardLocalizer["WorkspacePagesDescription"]</p>
            </div>

            <div class="flex items-center justify-between gap-3">
                <nav class="flex min-w-0 flex-1 items-center gap-2">
                    @foreach (var item in primaryItems)
                    {
                        <CustomButton
                            Color="@(IsMenuItemSelected(item) ? ThemeColor.Primary : ThemeColor.Default)"
                            Variant="@(IsMenuItemSelected(item) ? Variant.Solid : Variant.Outlined)"
                            Size="@Size.Medium"
                            OnClick="@(() => OnMenuItemSelected(item.Url))">
                            <StartContent>
                                <i class="@item.Icon"></i>
                            </StartContent>
                            <ChildContent>
                                <span class="hidden sm:inline">@item.Name</span>
                            </ChildContent>
                        </CustomButton>
                    }
                    @if (groupedItems.Any())
                    {
                        <LumexDropdown ShowArrow="@true"
                                       Radius="@Radius.Small">
                            <LumexDropdownTrigger>
                                <LumexButton
                                    Variant="@Variant.Outlined"
                                    IconOnly="true"
                                    Size="@Size.Medium">
                                    <StartContent>
                                        <i class="fa fa-arrow-down" aria-hidden="true"></i>
                                    </StartContent>
                                </LumexButton>
                            </LumexDropdownTrigger>
                            <LumexDropdownMenu Class="p-3">
                                @foreach (var group in groupedItems.Where(g => string.IsNullOrEmpty(g.Key)))
                                {
                                    foreach (var item in group)
                                    {
                                        <LumexDropdownItem
                                            Color="@(IsMenuItemSelected(item) ? ThemeColor.Primary : ThemeColor.Default)"
                                            Variant="@(IsMenuItemSelected(item) ? MenuVariant.Solid : MenuVariant.Outlined)"
                                            OnClick="@(() => OnMenuItemSelected(item.Url))">
                                            <StartContent>
                                                <span class="flex items-center gap-2">
                                                    <i class="@item.Icon"></i>
                                                </span>
                                            </StartContent>
                                            <ChildContent>
                                                @item.Name
                                            </ChildContent>
                                        </LumexDropdownItem>
                                    }
                                }
                                @foreach (var group in groupedItems.Where(g => !string.IsNullOrEmpty(g.Key)))
                                {
                                    <LumexDropdownItem
                                        ShowDivider="true"
                                        Disabled="true">
                                        @group.Key
                                    </LumexDropdownItem>
                                    foreach (var item in group)
                                    {
                                        <LumexDropdownItem
                                            Color="@(IsMenuItemSelected(item) ? ThemeColor.Primary : ThemeColor.Default)"
                                            Variant="@(IsMenuItemSelected(item) ? MenuVariant.Solid : MenuVariant.Outlined)"
                                            OnClick="@(() => OnMenuItemSelected(item.Url))">
                                            <StartContent>
                                                <span class="flex items-center gap-2">
                                                    <i class="@item.Icon"></i>
                                                </span>
                                            </StartContent>
                                            <ChildContent>
                                                @item.Name
                                            </ChildContent>
                                        </LumexDropdownItem>
                                    }
                                }
                            </LumexDropdownMenu>
                        </LumexDropdown>
                    }
                </nav>

                @if (settingsItem != null)
                {
                    <div class="shrink-0">
                        <CustomButton
                            Color="@(IsMenuItemSelected(settingsItem) ? ThemeColor.Primary : ThemeColor.Default)"
                            Variant="@(IsMenuItemSelected(settingsItem) ? Variant.Solid : Variant.Outlined)"
                            Size="@Size.Medium"
                            IconOnly="true"
                            OnClick="@(() => OnMenuItemSelected(settingsItem.Url))">
                            <StartContent>
                                <span class="flex items-center gap-2">
                                    <i class="@settingsItem.Icon"></i>
                                </span>
                            </StartContent>
                        </CustomButton>
                    </div>
                }
            </div>
        </div>
    </div>
</section>
