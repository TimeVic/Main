@using LumexUI.Common
@using TimeTracker.Api.Shared.Dto.Entity.Task

@inherits BaseReactiveComponent

<span class="@Class" title="@ButtonTitle">
    @if (IsCurrentTaskTimerActive)
    {
        <AppButton
            Variant="@Variant"
            Color="ComponentColor.Default"
            IconOnly="@IconOnly"
            Size="@Size"
            Disabled="@IsDisabled"
            OnClick="@StopTimeEntry">
            <StartContent>
                <i class="fa-solid fa-stop text-xs"></i>
            </StartContent>
            <ChildContent>
                @if (!IconOnly)
                {
                    <span class="task-tracker-button-text">@DashboardLocalizer["StopTimer"]</span>
                }
            </ChildContent>
        </AppButton>
    }
    else
    {
        <AppButton
            Variant="@Variant"
            Color="ComponentColor.Info"
            IconOnly="@IconOnly"
            Size="@Size"
            Disabled="@IsDisabled"
            OnClick="@StartTimeEntry">
            <StartContent>
                <i class="fa-solid fa-play text-xs"></i>
            </StartContent>
            <ChildContent>
                @if (!IconOnly)
                {
                    <span class="task-tracker-button-text">@DashboardLocalizer["StartTimer"]</span>
                }
            </ChildContent>
        </AppButton>
    }
</span>
