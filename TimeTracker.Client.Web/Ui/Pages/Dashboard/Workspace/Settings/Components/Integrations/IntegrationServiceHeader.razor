@using LumexUI.Common

<div class="flex w-full items-start justify-between gap-4">
    <div class="flex items-start gap-3">
        <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-slate-100 text-slate-700">
            <i class="@IconClass"></i>
        </div>
        <div>
            <p class="text-xs font-semibold uppercase tracking-[0.25em] text-slate-500">@DashboardLocalizer["IntegrationServiceHeader_Service"]</p>
            <h2 class="mt-1 text-lg font-semibold text-slate-900">@ServiceName</h2>
            <span class="@StatusClass">
                <i class="fa-solid @(IsActive ? "fa-link" : "fa-link-slash")"></i>
                @(IsActive ? DashboardLocalizer["IntegrationServiceHeader_Active"] : DashboardLocalizer["IntegrationServiceHeader_Inactive"])
            </span>
        </div>
    </div>
    <span title="@string.Format(DashboardLocalizer["IntegrationServiceHeader_HowToConnect"], ServiceName)">
        <AppButton
            IconOnly="true"
            Size="ComponentSize.Small"
            Color="ComponentColor.Primary"
            Variant="ButtonVariant.Light"
            Radius="ButtonRadius.Full"
            OnClick="@OnHelpClick">
            <ChildContent>
                <i class="fa-solid fa-circle-info"></i>
            </ChildContent>
        </AppButton>
    </span>
</div>

@code {
    [Parameter]
    public string ServiceName { get; set; } = string.Empty;

    [Parameter]
    public string IconClass { get; set; } = string.Empty;

    [Parameter]
    public bool IsActive { get; set; }

    [Parameter]
    public EventCallback OnHelpClick { get; set; }

    private string StatusClass =>
        IsActive
            ? "mt-3 inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700"
            : "mt-3 inline-flex items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-xs font-semibold text-slate-500";
}
