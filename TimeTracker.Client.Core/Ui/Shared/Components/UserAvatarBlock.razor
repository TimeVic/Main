@if (AvatarUrl != null)
{
    <img
        @key="AvatarKey"
        class="@AvatarClass"
        src="@AvatarUrl"
        alt="@AltText"
        title="@TitleText" />
}
else
{
    <span
        class="@AvatarClass"
        title="@TitleText">
        @ResolvedInitials
    </span>
}
