@using TimeTracker.Api.Shared.Dto.Model.Report
@using TimeTracker.Business.Extensions
@using Fluxor
@using TimeTracker.Web.Store.Client
@using TimeTracker.Web.Store.Project

@code {

    [Parameter]
    public ICollection<SummaryByUsersReportItemDto> Items { get; set; } = new List<SummaryByUsersReportItemDto>();

}

@* <FluentDataGrid TGridItem="SummaryByUsersReportItemDto" Items="@Items.AsQueryable()"> *@
@*     <PropertyColumn Title="User" Property="@(p => p.Name)" Sortable="false"/> *@
@*     <PropertyColumn Title="Duration" Property="@(p => p.Duration.ToReadableShortString())" Sortable="false"/> *@
@*     <PropertyColumn Title="Amount" Property="@(p => p.Amount.ToMoneyFormat())" Sortable="false"/> *@
@* </FluentDataGrid> *@
