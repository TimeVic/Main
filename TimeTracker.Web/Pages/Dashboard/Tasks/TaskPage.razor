@page "/board/task/{TaskId:guid}"

@using TimeTracker.Web.Pages.Dashboard.Shared
@using TimeTracker.Web.Pages.Dashboard.Shared.Tasks

@inherits BaseReactiveComponent
@layout Layout

<FluentStack Class="w-100" Orientation="Orientation.Vertical">
    <FluentToolbar Class="w-100">
        <FluentButton
            IconStart="@(new Icons.Filled.Size32.ArrowLeft())"
            Appearance="Appearance.Neutral"
            OnClick="@NavigateToTasksPage">
            To list
        </FluentButton>
    </FluentToolbar>

    @if (_isLoading)
    {
        <FluentProgressRing></FluentProgressRing>
    }
    else if (TaskId != null)
    {
        <UpdateTaskForm TaskId="TaskId.Value"/>
    }
</FluentStack>
