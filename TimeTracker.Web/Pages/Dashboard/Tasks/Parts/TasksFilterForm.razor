@using TimeTracker.Api.Shared.Dto.RequestsAndResponses.Dashboard.Tasks
@using Microsoft.AspNetCore.Components

@inherits BaseReactiveComponent

<FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.Start" Class="pa-2">
    
    <FluentTextField
        Value="@Filter.SearchString"
        ValueChanged="OnChangeSearchString"
        Placeholder="Write anything"/>
    
    <MembersDropDown
        Clearable="true"
        UserId="@(Filter.AssignedUserId ?? 0)"
        SelectedItemChanged="OnChangeAssignedUser"/>

    @* <FluentSpacer/> *@
    @* *@
    @* <YesNoDropDown *@
    @*     Placeholder="Only archived" *@
    @*     ValueChanged="OnChangeIsArchived" *@
    @*     Value="@Filter.IsArchived"/> *@

</FluentStack>

