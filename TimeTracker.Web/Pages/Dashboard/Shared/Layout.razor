@inherits BaseLayout

<RadzenContextMenu/>
<RadzenDialog />
<RadzenTooltip />
<RadzenNotification/>

@if (CommonState.Value.IsInitialized)
{
    <RadzenLayout>
        <MainHeader/>
        @if (IsShowMainMenu)
        {
            <NavMenu/>
        }
        <RadzenBody>
            <div class="w-container container-fluid">
                @Body
            </div>
            @if (!IsShowReCaptcha)
            {
                <style>
                    .grecaptcha-badge {
                        visibility: hidden;
                    }
                </style>
            }
        </RadzenBody>
    </RadzenLayout>    
}
