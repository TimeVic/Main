@inherits BaseLayout

@if (CommonState.Value.IsInitialized)
{
    <RadzenLayout>
        <MainHeader/>
        @if (IsShowMainMenu)
        {
            <NavMenu/>
        }
        <RadzenBody>
            <div class="w-container container-fluid">
                @Body
            </div>
            @if (!IsShowReCaptcha)
            {
                <style>
                    .grecaptcha-badge {
                        visibility: hidden;
                    }
                </style>
            }
        </RadzenBody>
        <RadzenFooter>
            <Footer></Footer>
        </RadzenFooter>
    </RadzenLayout>    
}

<RadzenContextMenu/>
<RadzenDialog />
<RadzenTooltip />
<RadzenNotification/>
