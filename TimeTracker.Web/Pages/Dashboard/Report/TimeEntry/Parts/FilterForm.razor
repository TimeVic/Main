@inherits BaseReactiveComponent

<FluentToolbar Class="pa-4 w-100" Orientation="Orientation.Horizontal">
    <FluentGrid Spacing="2" Justify="JustifyContent.SpaceBetween">
        <FluentGridItem>
            <ClientsDropDown
                Label="Client"
                Clearable="true"
                Value="@(_state.Value.Filter.ClientId ?? 0)"
                SelectedItemChanged="OnChangeClient"/>
        </FluentGridItem>
        <FluentGridItem>
            <ProjectsDropDown
                Label="Project"
                Clearable="true"
                Value="@(_state.Value.Filter.ProjectId ?? 0)"
                ClientId="@(_state.Value.Filter.ClientId ?? 0)"
                SelectedItemChanged="OnChangeProject"/>
        </FluentGridItem>
        <FluentGridItem>
            <FluentDatePicker
                Label="Date from"
                Value="@_state.Value.Filter.DateFrom"
                ValueChanged="OnChangeDateFrom"/>
        </FluentGridItem>
        <FluentGridItem>
            <FluentDatePicker
                Label="Date to"
                Value="@_state.Value.Filter.DateTo"
                ValueChanged="OnChangeDateTo"/>
        </FluentGridItem>
    </FluentGrid>
    <FluentGrid
        Spacing="2"
        Justify="JustifyContent.SpaceBetween"
        Class="mt-2">
        <FluentGridItem>
            <MembersDropDown
                Label="Member"
                Clearable="true"
                Class="w-100"
                UserId="@(_state.Value.Filter.UserId ?? 0)"
                SelectedItemChanged="OnChangeMember"/>
        </FluentGridItem>
        <FluentGridItem>
            <FluentTextField
                Label="Search"
                Class="w-100"
                Value="@_state.Value.Filter.Search"
                ValueChanged="OnChangeSearch"/>
        </FluentGridItem>
    </FluentGrid>
</FluentToolbar>
