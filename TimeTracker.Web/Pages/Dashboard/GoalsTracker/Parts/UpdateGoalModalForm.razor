@inherits BaseComponent

<MudDialog Style="min-width: 500px;">
    <DialogContent>
        <EditForm
            @ref="_form"
            @bind-IsValid="_isValid">
            <FluentTextField
                FullWidth="true"
                OnKeyUp="@OnKeyUp"
                T="string"
                Name="Name"
                Label="Goal"
                @bind-Value="model.Name"/>
            <MudSpacer/>
            <FluentTextField
                FullWidth="true"
                OnKeyUp="@OnKeyUp"
                T="int"
                Name="Goal"
                @bind-Value="model.NumberOfTimes"
                Label="Number of times to perform goal in a month"/>

        </EditForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@OnCloseModal">Cancel</MudButton>
        <MudButton
            ButtonType="MudBlazor.ButtonType.Submit"
            OnClick="@Submit"
            Disabled="@_isLoading"
            Color="Color.Primary">
            Save
        </MudButton>
    </DialogActions>
</MudDialog>
