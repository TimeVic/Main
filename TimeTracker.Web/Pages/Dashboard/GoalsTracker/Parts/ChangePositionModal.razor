@inherits BaseComponent

<MudDialog Style="min-width: 300px;">
    <DialogContent>
        <Dropzone Items="@Tracker.Items.ToList()">
            <MudCard Class="pa-2 mb-2 cursor-move">@context.Name</MudCard>
        </Dropzone>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@OnCloseModal">Cancel</MudButton>
        <MudButton
            ButtonType="MudBlazor.ButtonType.Submit"
            OnClick="@Submit"
            Disabled="@_isLoading"
            Color="Color.Primary">
            Save
        </MudButton>
    </DialogActions>
</MudDialog>
